1- select specific column
2- filter rows
3- combine multiple conditions

1- square bracket (for selecting columns)
2- boolen conditions (for filtering rows)

selecting columns
1- a series
2- dataframe multiple columns of data 

column = df["column name"]
subset = df["column1","column2",....]

filtering rows
boolean indexing

#based on a single conditions
filtered_rows = df[df["column"]>50000]

#multiple conditions
filtered_rows = df[(df["column"]>value) & (df["column"]<value1)]

